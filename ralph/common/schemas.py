from ninja import Schema


class BaseModel(Schema):

    """
    Base schema model to use across entire project.

    Configurations that should be applied to every schema, should be placed here.
    """

    ...
